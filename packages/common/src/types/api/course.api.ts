
export interface CourseBasicInfo {
  id: number;
  courseCode: string;
  courseName: string;
}

// Add other course-related API types here if needed in the future
// e.g., CreateCourseInput, UpdateCourseInput
